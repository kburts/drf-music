'use strict'

app = angular.module 'playlistApp'

app.controller 'AuthCtrl', ['$scope', ($scope) ->
    $scope.user = {}
    $scope.message = ""

    $scope.login = ->

    $scope.register = ->

    
]